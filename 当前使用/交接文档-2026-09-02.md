# 作品集交接文档｜2026-09-02

这份文档用于把当前作品集的状态、已确认决策和下一步边界交给后续修改者。它记录的是“现在是什么状态、下一步应该先做什么”，不代表所有方案都已经获得最终视觉确认。

## 1. 当前状态

- 项目目录：`/Users/runmeng.ma/Desktop/马润萌/portfolio-local-preview`
- 当前分支：`agent/visual-preview-digital-twin`
- 本地预览：

  ```bash
  cd /Users/runmeng.ma/Desktop/马润萌/portfolio-local-preview
  python3 -m http.server 4174
  ```

  访问 `http://127.0.0.1:4174/`。

- 正式站点：`https://www.renee.asia`
- 当前工作区包含较多未提交改动，禁止用 `git reset --hard`、`git checkout --` 等方式清理。先确认改动归属，再处理提交。
- 本轮暂不推送 GitHub，也暂不重新部署 Vercel。等最后一屏视觉确认后，与后续修改一起推送。

## 2. 这次明确的工作边界

### 不修改 04

用户这次没有要求改 04 的文案或结构。当前应把“最后一块视觉明显不搭”记录为待处理的视觉问题，而不是默认修改 04。

如果后续确实需要调整，优先只改字体层级、换行、间距、对齐和信息编排；不要先改写已经确认的 04 文案。只有视觉排版仍无法成立时，才把具体文案改动提出来单独确认。

### 最后一屏先排版、后动效

最后一屏当前的问题不是缺少动效，而是字体、留白和信息层级没有形成统一的收束。执行顺序固定为：

1. 先确定静态字体与版式；
2. 再检查桌面端和移动端的阅读顺序；
3. 最后恢复或调整自动播放动效；
4. 用户确认后，再和其他改动一起推送。

本轮生成的静态概念图只是评审参考，不是已确认的网页稿，也不是正式运行素材：

`/Users/runmeng.ma/.codex/generated_images/01a05707-e8ef-7a73-b75c-48b5b2dcb7c1/exec-c83d7137-7173-4d86-990a-0f1ab10ebd9e.png`

概念图采用“左侧叙事、右侧对话、底部轻量收束”的方向。它可以作为候选视觉结构，但在用户确认前不得直接接入正式页面。

## 3. 当前页面结构与已确认内容

### 开屏 / Entry

- 用户倾向：保留 `Renee` 名字，不再突出开屏 slogan。
- 蓝色小球应是字母上的引导点，落在字母上并自然弹跳；不是水波纹、墨滴或发光粒子。
- C 方案的独立原型已放在 `待确认/开屏/`，目前仍应视为独立试稿；正式 `index.html` 还保留现有入口结构，不能把试稿误认为已上线。
- 已验证的关键问题：不能让小球改变最终字母间距，不能用粗圆环制造触发反馈，动效要避免卡顿。

相关文件：

- `待确认/开屏/preview-06-letter-reveal.html`
- `待确认/开屏/preview-07-ball-dot.html`
- `index.html` 的 `.entry` 区域
- `app.js` 与 `styles.css` 的入口交互样式

### 01 EXPERIENCE / 实习经历

- 主标题：`实习经历`
- 副标题：`从设计到产品`
- 采用横向滑动卡片；不恢复独立的图片式 Experience 插画区。
- 真实经历和项目事实以 `当前使用/确认文案.md` 为基准；下方“最新文案”一节列出的蔚来描述仍需确认是否写回正式网页。

### 02 WORKS / 精选项目

- 移动端阅读顺序必须是：先显示 02 标题与项目说明文案，再显示项目卡。
- 卡片点击直接进入详情页，不以抽屉作为主路径。
- 当前详情页入口已存在：
  - `projects/a-pillar/`
  - `projects/weight-system/`
  - `projects/pet-collar/`
  - `projects/poopnow/`
- `PoopNow` 已上线，可直接体验；它是独立的微信小程序项目，不是宠物硬件。
- “A 柱补盲”与 `BSV / DOW` 是同一个项目的不同表述，不要拆成两个项目，也不要推断包含关系。
- 项目详情页后续可做颜色靠拢，但要保持页面整体的纸张、黑色文字和钴蓝线语言；需要单独视觉验收。

### 03 EDUCATION / 教育经历

- 默认展示两段学历、硕士荣誉和两项代表性本科荣誉；其余荣誉与志愿经历折叠展示，不删除。
- 移动端曾发现文案与蓝线重合；后续验收时要检查教育 connector 的位置、行高和断行，不能只看桌面端。
- 不要为了修线条重写教育事实。

### CONTACT / 联系我

- 标题：`期待把判断做成真正落地的产品。`
- 状态：`正在寻找 2027 届智能硬件或智能座舱产品经理校招机会。`
- 联系入口保持邮箱、手机 / 微信和简历 PDF；具体入口样式可继续微调。

### 04 ASK ME / 和我聊聊

当前正式页面的 04 结构位于 `index.html` 第 309 行附近，包含：

- 04 说明文案；
- 数字分身对话区；
- `问项目 / 问方法 / 问经历` 三个引导入口；
- 输入框与发送按钮；
- 下方 `NEXT / 继续往前` 结尾段落。

当前确认文案见 `当前使用/确认文案.md`，包括初始回复、三个默认问题和输入提示。未经用户再次确认，不要把这部分改成更像产品后台的卡片式 UI。

## 4. 最后一屏的视觉诊断

问题集中在现有 `styles.css` 的多轮覆盖，而不是单个字号：

- `.digital-twin.ask-me`、`.twin-console` 和 `.ending-scene` 有多段历史样式叠加；
- `ending-scene` 当前使用较大的最小高度，并让三段文案在大面积空间中居中切换，导致结尾显得空、重，和前面的编辑感纸张语言脱节；
- 对话区虽然取消了外框，但内部规则线、引导按钮和结尾区没有形成同一层级；
- 结尾应避免再次出现厚边框、粗圆环、发光效果和巨大的孤立居中文字。

建议的候选方向是：保留 04 的语义，压缩结尾纵向高度；让左侧承担叙事，右侧承担对话，底部以细蓝线和较小的 `NEXT` 文案收束。这个方向只作为视觉讨论稿，未获确认前不实现。

## 5. 最新待同步的蔚来实习文案

以下是用户提供的最新版本。它已经进入交接记录，但尚未自动写回 `index.html` 或 `当前使用/确认文案.md`，避免在未确认前造成事实库和网页双重变更。

### AI 产品定义｜车载导航 AI 产品规划

基于 FY 用户真实出行场景，结合行业车企、NIO 主线及高德/百度/腾讯图商能力对标，识别模糊/复合搜索、多 POI 连续行程、地点记忆与补能决策等核心 Gap，完成随心搜、灵犀行程、地址妙记等 P0/P1 产品规划，并进一步抽象 Intent、Trip State、Memory、Context、Decision / Orchestration 等共性能力，梳理 NOMI 主线复用、FY 新增及图商能力依赖，明确后续产品实现与能力建设路径。

### 座舱侧功能 Owner｜盲点影像与开门预警

承接 G1.3 改款车型 P0 感知影像需求，独立负责 BSV 车型级新增及 DOW 规则优化与适配；基于车辆感知能力与实际用车风险场景，完成触发/退出、左右视图、风险提示、优先级冲突及异常恢复等功能规则与交互方案设计，输出完整 PRD 并明确感知能力边界及跨域交互逻辑，协同感知、仪表、研发等团队完成方案评审、收口并推进开发。

### 数据体系设计｜360 影像

针对泊车、倒车、窄道等高频影像场景缺少有效数据支撑产品判断的问题，从视角偏好、操作路径、规则联动、异常及改版验证等决策问题反推数据需求，拆解 6 类核心分析目标，独立设计调起/退出、视图切换、设置、规则阻断、联动及异常等 22 个关键事件，并主动剔除低价值指标；推动方案完成跨团队评审，为后续用户行为分析、体验问题定位及版本效果验证建立数据基础。

## 6. 数字分身与事实库

- 后端入口：`api/chat.js`，前端调用 `/api/chat`。
- 生产环境至少需要：`DEEPSEEK_API_KEY`、独立的 `VISITOR_HASH_SECRET`；`DEEPSEEK_MODEL` 可选。
- `DEEPSEEK_API_KEY` 与 `VISITOR_HASH_SECRET` 不能复用，任何值都不能写入前端、截图或提交到 Git。
- `api/chat.js` 已限制单条消息长度、历史消息数、每分钟请求数和访客总回复数；回答默认控制在 1—3 句话，避免写成啰嗦的面试标准答案。
- 事实边界以 `api/chat.js` 中的 `SYSTEM_PROMPT` 与 `当前使用/确认文案.md` 为准：严格区分“规划完成”“推进开发”“预立项”“已立项进入开发”和“已经上线”。
- 个人问题可直接回答当前已确认口径：目前全身心以工作和职业成长为重心，短期内不考虑结婚和生育，这不会影响职业投入和稳定性。

## 7. 后续执行顺序

1. 先让用户确认最后一屏的静态版式方向；不把概念图当成最终稿。
2. 只修改最后一屏的字体、排版、留白和对齐，保持 04 文案不变，除非另行确认。
3. 用桌面宽度和移动端宽度分别检查 02 顺序、03 蓝线重合和最后一屏阅读节奏。
4. 再调整最后一屏自动播放：进入可视区后自动播放，播放完成后保留全部文案；支持 `prefers-reduced-motion` 静态展示。
5. 再评估开屏 C 方案是否接入正式页面，并检查蓝点不改字距、不出现水波纹或粗圆环。
6. 做一次项目详情页、API 错误态、移动端和正式站点回归检查。
7. 用户确认后，统一提交并推送 GitHub，再部署 Vercel。

## 8. 禁止事项

- 未确认前，不把 `待确认/` 或 `废案/` 的整张视觉稿接入正式网页。
- 不删除项目详情页、教育折叠内容或已有事实。
- 不把 PoopNow 写成宠物硬件项目，不把 A 柱补盲与 BSV / DOW 拆成两个项目。
- 不为了填满最后一屏增加装饰性人物、粗线条、圆环、发光渐变或循环粒子。
- 不在本轮视觉确认前推送、部署或重置工作区。
