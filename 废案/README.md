# 废案与历史资料

这里的内容默认不再用于当前网页。只有需要追溯旧方向时才查看，不要从这里直接复制图、文案或代码回正式页面。

- `设计过程稿/`：被替代的候选图、旧原型、旧索引、人物换装和参考素材
- `旧网页素材/`：正式主页没有引用的旧图片、字体和项目卡拆分素材
- `旧独立预览/`：已经被当前主页取代的独立审阅页
- `生成脚本/`：只服务于旧整页候选稿的脚本
- `历史文档/`：旧交接记录；其中可能包含已经过时的路径和文案

废案未删除，可随时找回；正式修改入口是 `../当前使用/README.md`。
