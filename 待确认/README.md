# 待确认

这里存放还没有最终决定的方案，不接入正式网页。

- `开屏/`：2026-08-21 的开屏视觉与交互探索
- `结尾动效C/`：2026-08-22 的结尾分帧、精灵图与交互试稿
- `整页视觉/`：尚未锁定的整页视觉稿
- `移动端/`：移动端历史迭代稿；移动端当前仍未正式验收

确认采用后，先移入 `../当前使用/` 并更新清单；明确否决或被替代后，移入 `../废案/`。
